using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerCollideEnemy : MonoBehaviour
{
    //If dashing is true and collision on enemy, enemy dies
    //Add freeze frame to have more impact
    //particles too and sfx
    //If dashing is false and collision on enemy, player gets stunned, enemy lives
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
